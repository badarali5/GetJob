spring.application.name=GetJob

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/jobfinder}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:badar512}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=8081
jwt.secret=${JWT_SECRET:9f3c2d4e7a6b8c1d5e9f0a2b4c6d8e1f3a5b7c9d0e2f4a6b8c1d3e5f7a9b0c2d}
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:8080,http://localhost:5173,http://localhost:8082}
jsearch.api.key=${RAPID_API_KEY:b2d573306dmsh94f1e8659ce9dbcp1ed860jsn85deba6d519f}
jsearch.api.host=${JSEARCH_API_HOST:jsearch.p.rapidapi.com}
jsearch.api.url=https://jsearch.p.rapidapi.com/search
spring.sql.init.mode=${SPRING_SQL_INIT_MODE:always}
spring.sql.init.continue-on-error=${SPRING_SQL_INIT_CONTINUE_ON_ERROR:true}
spring.flyway.enabled=${SPRING_FLYWAY_ENABLED:false}

# Multipart file upload settings
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

app.storage.local-dir=${APP_STORAGE_LOCAL_DIR:${user.dir}/uploads/resumes}
app.storage.s3.enabled=${APP_STORAGE_S3_ENABLED:true}
app.storage.s3.bucket=${AWS_S3_BUCKET:}
app.storage.s3.region=${AWS_REGION:}
app.storage.s3.public-base-url=${AWS_S3_PUBLIC_BASE_URL:}
app.storage.s3.prefix=${AWS_S3_PREFIX:resumes}